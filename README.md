Screenshots referenced from the `feat/publisher-device-picker` pull request body.

Captured from `apps/sandbox` (`/moq-publisher/?fake&synthetic`) rendering the real
`<publisher-skin>`, with `navigator.mediaDevices.enumerateDevices` stubbed to report
two cameras and two microphones (and one of each for the single-device shots).

This branch carries no source — delete it once the pull request is merged or closed.
